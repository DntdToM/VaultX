def kmp_search(text, pattern):
    """
    Thuật toán KMP dùng để tìm chuỗi con trong một chuỗi lớn hơn.

    Trong dự án, KMP được dùng để lọc danh sách tài khoản trên Dashboard.
    Khác với Trie chỉ tìm ở đầu chuỗi, KMP có thể tìm từ khóa nằm ở giữa domain, username hoặc ghi chú với độ phức tạp O(n + m).

    Args:
        text: Chuỗi gốc cần tìm kiếm.
        pattern: Chuỗi con cần kiểm tra.

    Returns:
        True nếu tìm thấy pattern trong text, ngược lại là False.
    """

    pass
